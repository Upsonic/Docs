---
title: Legacy Version - Python 3.6
icon: "clock"
iconType: "solid"
---

We provide a legacy version for **Python 3.6**. You can install and use but some elements now working in legacy:

- `return_openai_llm`
- `return ollama_llm`

these functions not working in legacy.

```console
pip install upsonic_legacy==0.26.0
```