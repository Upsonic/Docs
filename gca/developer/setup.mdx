
---
title: Setup
---


## Core Installation from `pip`

GCA written in Python so you can start an configurable development envinronment via `pip` python package manager.

```bash
pip install `gpt-computer-assistant[base]`
```

<Info>
  You'll need Python
  [3.10](https://www.python.org/downloads/release/python-3100/) or
  [3.11](https://www.python.org/downloads/release/python-3110/). Run `python
  --version` to check yours.

For this requirement we suggest to use [Conda Navigator](https://www.anaconda.com/products/navigator)
</Info>

## Extensions

In GCA we develop an core system and additionaly features that can help in some cases. So you can chose and install any specific extension.


[API](/gca/developer/api) ability

```bash
pip install 'gpt-computer-assistant[api]'
```

[Agentic](/gca/developer/agentic) ability

```bash
pip install 'gpt-computer-assistant[agentic]'
```

[Local TTS](/gca/developer/local_tts) ability

```bash
pip install 'gpt-computer-assistant[local_tts]'
```

[Local STT](/gca/developer/local_stt) ability

```bash
pip install 'gpt-computer-assistant[local_stt]'
```

[Wake Word](/gca/developer/wake_word)

```bash
pip install 'gpt-computer-assistant[wakeword]'
```


## Experimental Exe and DMG

To use GCA you can use our exe and dmg builds as well. But some features are not supported now. We are working on it. So they are experimental but you can use as a start.

Coming soon